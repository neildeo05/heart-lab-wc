# Word Cloud based on multiple GoFundMe pages
The file wc.py is a python script that uses the WordCloud library

The directory wc-custom contains a customized word cloud that I build in C++ and Python. In order to build the wordcloud, run 
```
make build
```

## GoFundMe Links: 

